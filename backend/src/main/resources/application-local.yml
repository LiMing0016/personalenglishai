spring:
  config:
    activate:
      on-profile: local

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    reactor.netty: DEBUG

assistant:
  chatkit:
    workflow-id: ${OPENAI_CHATKIT_WORKFLOW_ID:}
