<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.personalenglishai.backend.mapper.subscription.UserSubscriptionMapper">

    <resultMap id="UserSubscriptionResultMap" type="com.personalenglishai.backend.entity.subscription.UserSubscription">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="plan_code" property="planCode"/>
        <result column="status" property="status"/>
        <result column="current_period_start" property="currentPeriodStart"/>
        <result column="current_period_end" property="currentPeriodEnd"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findLatestByUserId" resultMap="UserSubscriptionResultMap">
        SELECT id,
               user_id,
               plan_code,
               status,
               current_period_start,
               current_period_end,
               created_at,
               updated_at
        FROM user_subscription
        WHERE user_id = #{userId}
        LIMIT 1
    </select>

    <insert id="upsert" parameterType="com.personalenglishai.backend.entity.subscription.UserSubscription">
        INSERT INTO user_subscription (
            user_id,
            plan_code,
            status,
            current_period_start,
            current_period_end
        )
        VALUES (
            #{userId},
            #{planCode},
            #{status},
            #{currentPeriodStart},
            #{currentPeriodEnd}
        )
        ON DUPLICATE KEY UPDATE
            plan_code = VALUES(plan_code),
            status = VALUES(status),
            current_period_start = VALUES(current_period_start),
            current_period_end = VALUES(current_period_end),
            updated_at = CURRENT_TIMESTAMP
    </insert>

</mapper>
