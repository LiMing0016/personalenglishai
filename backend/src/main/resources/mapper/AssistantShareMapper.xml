<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.personalenglishai.backend.mapper.assistant.AssistantShareMapper">

    <resultMap id="AssistantShareResultMap" type="com.personalenglishai.backend.entity.assistant.AssistantShare">
        <id column="id" property="id"/>
        <result column="share_token" property="shareToken"/>
        <result column="conversation_uid" property="conversationUid"/>
        <result column="owner_user_id" property="ownerUserId"/>
        <result column="title_snapshot" property="titleSnapshot"/>
        <result column="messages_snapshot" property="messagesSnapshot"/>
        <result column="created_at" property="createdAt"/>
        <result column="revoked_at" property="revokedAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.personalenglishai.backend.entity.assistant.AssistantShare"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO assistant_share (
            share_token, conversation_uid, owner_user_id, title_snapshot, messages_snapshot, created_at
        )
        VALUES (
            #{shareToken}, #{conversationUid}, #{ownerUserId}, #{titleSnapshot}, #{messagesSnapshot}, NOW()
        )
    </insert>

    <select id="findActiveByToken" resultMap="AssistantShareResultMap">
        SELECT id, share_token, conversation_uid, owner_user_id, title_snapshot, messages_snapshot, created_at, revoked_at
        FROM assistant_share
        WHERE share_token = #{shareToken}
          AND revoked_at IS NULL
        LIMIT 1
    </select>

    <select id="findActiveByConversationOwned" resultMap="AssistantShareResultMap">
        SELECT id, share_token, conversation_uid, owner_user_id, title_snapshot, messages_snapshot, created_at, revoked_at
        FROM assistant_share
        WHERE owner_user_id = #{ownerUserId}
          AND conversation_uid = #{conversationUid}
          AND revoked_at IS NULL
        ORDER BY id DESC
        LIMIT 1
    </select>

    <update id="revokeOwned">
        UPDATE assistant_share
        SET revoked_at = #{revokedAt}
        WHERE share_token = #{shareToken}
          AND owner_user_id = #{ownerUserId}
          AND revoked_at IS NULL
    </update>
</mapper>
