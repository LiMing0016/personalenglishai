<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.personalenglishai.backend.mapper.admin.AdminUserRoleMapper">
    <resultMap id="AdminUserRoleResultMap" type="com.personalenglishai.backend.entity.admin.AdminUserRole">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="role" property="role"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="selectByUserId" resultMap="AdminUserRoleResultMap">
        SELECT id, user_id, role, created_at, updated_at
        FROM admin_user_role
        WHERE user_id = #{userId}
        ORDER BY role ASC
    </select>

    <select id="selectRoleNamesByUserId" resultType="string">
        SELECT role FROM admin_user_role WHERE user_id = #{userId} ORDER BY role ASC
    </select>

    <insert id="insert">
        INSERT INTO admin_user_role (user_id, role)
        VALUES (#{userId}, #{role})
    </insert>

    <delete id="deleteByUserId">
        DELETE FROM admin_user_role WHERE user_id = #{userId}
    </delete>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(*) FROM admin_user_role WHERE user_id = #{userId}
    </select>
</mapper>
