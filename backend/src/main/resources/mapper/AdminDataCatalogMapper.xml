<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.personalenglishai.backend.mapper.admin.AdminDataCatalogMapper">

    <select id="selectTables" resultType="java.util.LinkedHashMap">
        SELECT table_name AS tableName,
               table_comment AS tableComment,
               table_rows AS rowCount
        FROM information_schema.tables
        WHERE table_schema = DATABASE()
          AND table_type = 'BASE TABLE'
        ORDER BY table_name ASC
    </select>

    <select id="selectColumns" resultType="java.util.LinkedHashMap">
        SELECT column_name AS name,
               column_type AS type,
               is_nullable AS nullable,
               column_default AS defaultValue,
               column_comment AS comment
        FROM information_schema.columns
        WHERE table_schema = DATABASE()
          AND table_name = #{tableName}
        ORDER BY ordinal_position ASC
    </select>

    <select id="selectIndexes" resultType="java.util.LinkedHashMap">
        SELECT index_name AS name,
               GROUP_CONCAT(column_name ORDER BY seq_in_index SEPARATOR ', ') AS columns,
               CASE WHEN MIN(non_unique) = 0 THEN TRUE ELSE FALSE END AS uniqueIndex
        FROM information_schema.statistics
        WHERE table_schema = DATABASE()
          AND table_name = #{tableName}
        GROUP BY index_name
        ORDER BY CASE WHEN index_name = 'PRIMARY' THEN 0 ELSE 1 END, index_name ASC
    </select>

    <select id="selectForeignKeys" resultType="java.util.LinkedHashMap">
        SELECT constraint_name AS name,
               table_name AS tableName,
               column_name AS columnName,
               referenced_table_name AS referencedTableName,
               referenced_column_name AS referencedColumnName
        FROM information_schema.key_column_usage
        WHERE table_schema = DATABASE()
          AND table_name = #{tableName}
          AND referenced_table_name IS NOT NULL
        ORDER BY constraint_name ASC, ordinal_position ASC
    </select>

    <select id="selectAllForeignKeys" resultType="java.util.LinkedHashMap">
        SELECT constraint_name AS name,
               table_name AS tableName,
               column_name AS columnName,
               referenced_table_name AS referencedTableName,
               referenced_column_name AS referencedColumnName
        FROM information_schema.key_column_usage
        WHERE table_schema = DATABASE()
          AND referenced_table_name IS NOT NULL
        ORDER BY table_name ASC, constraint_name ASC, ordinal_position ASC
    </select>

    <select id="selectLatestAt" resultType="object">
        SELECT MAX(`${timeColumn}`)
        FROM `${tableName}`
    </select>

</mapper>
