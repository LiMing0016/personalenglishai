<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.personalenglishai.backend.mapper.UserProfileMapper">

    <resultMap id="UserProfileResultMap" type="com.personalenglishai.backend.entity.UserProfile">
        <id column="user_id" property="userId"/>
        <result column="study_stage" property="studyStage"/>
        <result column="ai_mode" property="aiMode"/>
    </resultMap>

    <select id="findByUserId" resultMap="UserProfileResultMap">
        SELECT user_id, study_stage, ai_mode
        FROM user_profile
        WHERE user_id = #{userId}
    </select>

    <insert id="insert">
        INSERT INTO user_profile (user_id, study_stage, ai_mode)
        VALUES (#{userId}, #{studyStage}, #{aiMode})
    </insert>

    <update id="updateStageAndAiMode">
        UPDATE user_profile
        SET study_stage = #{studyStage},
            ai_mode = #{aiMode}
        WHERE user_id = #{userId}
    </update>

</mapper>

